#include <stdio.h>
#include <math.h>
int main()
{
    int N, K, result;
    printf("");
    scanf("%d", &N);
    printf("");
    scanf("%d", &K);
    result = pow(N, K);
    printf("%d",result);
    return 0;
}
